# su26-ai301-contribution
## Phase I: Issue Selection

### Status

Phase I Complete

### Issue

https://github.com/marketcalls/openalgo/issues/889

### Why I Chose This Issue

I chose this issue because it has a clear and focused scope: improving inconsistent empty state UI patterns across the frontend. Some pages currently show plain text when there is no data, while other pages use a more polished empty state with an icon, heading, and description.

This issue matters because consistent empty states make the application feel more professional and easier to understand for users. I chose it because it is labeled as a good first issue, uses React/TypeScript, and gives me a realistic first open-source contribution without requiring heavy backend or environment setup.

From reading the issue, I understand that the expected contribution is to review the existing empty state pattern in `StrategyIndex.tsx`, then apply a similar icon, heading, and description pattern to the empty states in `MarketTimings.tsx` and `Search.tsx`.
